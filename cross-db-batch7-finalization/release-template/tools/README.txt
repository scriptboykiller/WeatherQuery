OPTIONAL PERFORMANCE TOOL

The initial release does not include sql-performance-comparator.jar.

When delivered, place it at:

tools\sql-performance-comparator.jar

Then configure performance-tool.yml and enable the performance phase.
Core validation, Cross DB comparison and Excel do not require this JAR.
